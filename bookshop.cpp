#include<bits/stdc++.h>
#define MOD (1000000007U)
#define FOR(a) (for (int i=0; i < a; ++i))
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  int n, x;
  cin >> n >> x;

  int h[n], s[n];
  FOR(n) cin >> h[i];
  FOR(n) cin >> s[i];

  
  return 0;
}
